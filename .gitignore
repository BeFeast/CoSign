# dependencies
node_modules

# build output
dist
*.tsbuildinfo

# tooling / local state
.wrangler
.vite

# env / secrets
.env
.env.*
!.env.example

# design-tool artifacts (dropped in for reference, not source)
*.zip
.uiux_unzip

# os
.DS_Store
